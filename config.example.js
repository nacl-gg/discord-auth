export const redirect_uri = ""; // cloudflare worker URL

export const bot_token = ""; // discord bot token
export const client_id = ""; // discord client id
export const client_secret = ""; // discord client secret

export const guild_id = ""; // discord guild id
export const role_id = ""; // discord role id in the guild

export const log_webhook_id = ""; // discord webhook id (bit before the slash)
export const log_webhook_token = ""; // discord webhook token (bit after the slash)
